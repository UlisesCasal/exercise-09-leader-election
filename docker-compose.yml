services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: noderegistry
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d noderegistry"]
      interval: 5s
      timeout: 3s
      retries: 5

  node-1:
    build: .
    ports:
      - "8081:8080"
    environment:
      NODE_ID: "1"
      DATABASE_URL: postgresql://user:password@db:5432/noderegistry
      PEERS: "2:http://node-2:8080,3:http://node-3:8080"
    depends_on:
      db:
        condition: service_healthy

  node-2:
    build: .
    ports:
      - "8082:8080"
    environment:
      NODE_ID: "2"
      DATABASE_URL: postgresql://user:password@db:5432/noderegistry
      PEERS: "1:http://node-1:8080,3:http://node-3:8080"
    depends_on:
      db:
        condition: service_healthy

  node-3:
    build: .
    ports:
      - "8083:8080"
    environment:
      NODE_ID: "3"
      DATABASE_URL: postgresql://user:password@db:5432/noderegistry
      PEERS: "1:http://node-1:8080,2:http://node-2:8080"
    depends_on:
      db:
        condition: service_healthy
